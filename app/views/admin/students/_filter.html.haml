= simple_form_for :filter, url: admin_students_path, method: :get do |f|
  = hidden_field_tag :search, params[:search]

  = filter_by(f, :school_class, school_classes_by_university(current_university), t('.filters.by_school_class'))
